apiVersion: v1
kind: Service
metadata:
  name: {{ include "fraud-detection-feast.fullname" . }}
  labels:
    {{- include "fraud-detection-feast.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fraud-detection-feast.selectorLabels" . | nindent 4 }}
